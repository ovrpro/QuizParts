# Changesets

When you make a change that affects a publishable package, run `yarn changeset` and follow the prompts to add a changeset. When the release workflow runs, it will version and publish based on these changesets.

Publishable packages: `@quizparts/schema`, `@quizparts/core`, `@quizparts/react`, `@quizparts/theme`.
